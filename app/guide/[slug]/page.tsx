import type {Metadata} from "next";import {notFound} from "next/navigation";import {DocsShell,DocBody} from "../DocsShell";import {docs,getDoc} from "../docs";
export function generateStaticParams(){return docs.filter(d=>d.slug!=="concepts").map(d=>({slug:d.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const doc=getDoc(slug);if(!doc)return{};return {title:`${doc.title} · AI 原生软件开发生命周期中文手册`,description:doc.summary,alternates:{canonical:`/guide/${doc.slug}`},openGraph:{title:doc.title,description:doc.summary,images:[]},twitter:{card:"summary",title:doc.title,description:doc.summary,images:[]}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const doc=getDoc(slug);if(!doc)notFound();return <DocsShell doc={doc}><DocBody doc={doc}/></DocsShell>}
