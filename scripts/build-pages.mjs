import {readFileSync,writeFileSync,mkdirSync,rmSync,cpSync} from "node:fs";
import {join} from "node:path";

const root=new URL("../",import.meta.url).pathname;
const out=join(root,"pages-dist");
const siteUrl="https://huiyonghkw.github.io/ai-native-sdlc-guide";
const articleUrl="https://claude.com/blog/the-ai-native-sdlc-playbook";
const officialDocsUrl="https://code.claude.com/docs/en/overview";
rmSync(out,{recursive:true,force:true});
mkdirSync(out,{recursive:true});

let source=readFileSync(join(root,"app/guide/docs.ts"),"utf8");
source=source
  .replace(/^export type[^\n]+\n/gm,"")
  .replace(/^export const getDoc[^\n]+\n/gm,"")
  .replaceAll("export const ","const ")
  .replace("const docs:Doc[]=","const docs=");
const {docs,docGroups,templateFiles}=Function(`${source};return {docs,docGroups,templateFiles}`)();

const esc=value=>String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
const docText=doc=>[doc.title,doc.summary,...doc.sections.flatMap(section=>[section.heading,...(section.paragraphs||[]),...(section.bullets||[]),section.code||"",section.note||""])].join("");
const readMinutes=doc=>Math.max(3,Math.ceil(docText(doc).replace(/\s/g,"").length/420));
const docHref=(scope,doc)=>scope==="shallow"?(doc.slug==="concepts"?"index.html":`${doc.slug}/index.html`):(doc.slug==="concepts"?"../index.html":`../${doc.slug}/index.html`);
const sourceTemplateHref=(prefix,file)=>`${prefix}templates/${file}`;
const top=(prefix,active="docs")=>`<header class="topbar"><a class="brand" href="${prefix}index.html">AI 原生 SDLC</a><nav aria-label="主导航"><a class="${active==="product"?"active":""}" href="${prefix}index.html">产品</a><a class="${active==="docs"?"active":""}" href="${prefix}guide/index.html">文档</a><a class="${active==="templates"?"active":""}" href="${prefix}guide/templates/index.html">模板</a><a class="${active==="sources"?"active":""}" href="${prefix}guide/sources/index.html">来源</a><a href="https://github.com/huiyonghkw/ai-native-sdlc-guide" target="_blank" rel="noreferrer">GitHub ↗</a></nav></header>`;
const head=(title,description,prefix,canonicalPath,includeImage=false)=>`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}">${includeImage?`<meta property="og:image" content="${siteUrl}/og.png">`:""}<meta name="twitter:card" content="${includeImage?"summary_large_image":"summary"}"><link rel="canonical" href="${siteUrl}/${canonicalPath}"><link rel="stylesheet" href="${prefix}assets/styles.css"></head><body>`;
const footer=`<footer class="footer docs-footer shell"><p>会勇禾口王的 AI 笔记 · @huiyonghkw</p><p><a href="https://github.com/huiyonghkw/ai-native-sdlc-guide" target="_blank" rel="noreferrer">GitHub 仓库 ↗</a> · 非官方中文学习手册 · 请以英文原文为准</p></footer>`;
const searchData=JSON.stringify(docs.map(doc=>({slug:doc.slug,number:doc.number,title:doc.title,summary:doc.summary,text:docText(doc)}))).replaceAll("<","\\u003c");
const searchMarkup=prefix=>`<div class="search-box"><label for="doc-search">搜索手册</label><div class="search-input-wrap"><input id="doc-search" type="search" placeholder="搜索主题、文件或术语" autocomplete="off" aria-controls="doc-search-results"><kbd>/</kbd></div><div class="search-results" id="doc-search-results" role="listbox" aria-label="搜索结果" hidden></div></div><script>(()=>{const input=document.getElementById("doc-search"),results=document.getElementById("doc-search-results"),docs=${searchData},base="${prefix}guide/";if(!input||!results)return;const render=()=>{const q=input.value.trim().toLocaleLowerCase();if(!q){results.innerHTML="";results.hidden=true;return}const hits=docs.filter(d=>d.text.toLocaleLowerCase().includes(q)).slice(0,6);results.hidden=false;results.innerHTML=hits.length?hits.map(d=>'<a href="'+base+(d.slug==="concepts"?'index.html':d.slug+'/index.html')+'" role="option"><span>'+d.number+'</span><b>'+d.title+'</b><small>'+d.summary+'</small></a>').join(""):"<p>没有找到匹配内容</p>"};input.addEventListener("input",render);document.addEventListener("keydown",event=>{if(event.key==="/"&&!/^(INPUT|TEXTAREA|SELECT)$/.test(event.target.tagName)){event.preventDefault();input.focus()};if(event.key==="Escape"){input.value="";render();input.blur()}})})();</script>`;
const behaviorMarkup=`<script>(()=>{document.querySelectorAll("[data-copy-code]").forEach(button=>button.addEventListener("click",async()=>{const code=button.parentElement.querySelector("code")?.textContent||"";try{await navigator.clipboard.writeText(code);button.textContent="已复制";setTimeout(()=>button.textContent="复制",1400)}catch{window.prompt("复制这段内容",code)}}));const share=document.querySelector("[data-share-page]");const copy=document.querySelector("[data-copy-page]");copy?.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(location.href);copy.textContent="已复制链接";setTimeout(()=>copy.textContent="复制页面链接",1600)}catch{window.prompt("复制这条链接",location.href)}});share?.addEventListener("click",async()=>{if(navigator.share){await navigator.share({title:"AI 原生软件开发生命周期中文手册",text:"代码写快以后，研发流程为什么反而堵了？",url:location.href})}else copy?.click()})})();</script>`;
const sidebar=(current,scope,prefix)=>`<aside class="sidebar" aria-label="文档目录">${searchMarkup(prefix)}<div class="sidebar-nav">${docGroups.map(group=>`<div class="sidebar-group"><p class="sidebar-group-title">${group.label}</p>${group.slugs.map(slug=>{const doc=docs.find(item=>item.slug===slug);return doc?`<a class="${doc.slug===current?"current":""}" href="${docHref(scope,doc)}"><span>${doc.number}</span>${esc(doc.title.split("：")[0])}</a>`:""}).join("")}</div>`).join("")}</div><div class="sidebar-source"><small>英文原文</small><a href="${articleUrl}" target="_blank" rel="noreferrer">Claude · 2026-08-21 ↗</a></div></aside>`;
const section=(item,index)=>`<section class="doc-section" id="section-${index+1}"><h2>${esc(item.heading)}</h2>${(item.paragraphs||[]).map(paragraph=>`<p>${esc(paragraph)}</p>`).join("")}${item.bullets?`<ul>${item.bullets.map(bullet=>`<li>${esc(bullet)}</li>`).join("")}</ul>`:""}${item.code?`<div class="code-block"><pre><code>${esc(item.code)}</code></pre><button class="copy-code" type="button" data-copy-code>复制</button></div>`:""}${item.note?`<aside class="note"><b>注意</b><p>${esc(item.note)}</p></aside>`:""}</section>`;
const templateDownloads=(prefix)=>`<section class="template-downloads" aria-labelledby="template-downloads-title"><div class="template-downloads-head"><span>DOWNLOADABLE STARTERS</span><h2 id="template-downloads-title">下载一套可直接改的起始文件</h2><p>这些文件是试点骨架，不是生产配置。下载后放进自己的仓库，按责任、权限和合规要求继续修改。</p></div><div class="template-grid">${templateFiles.map(file=>`<a class="template-card" href="${sourceTemplateHref(prefix,file.file)}" download><div><code>${esc(file.label)}</code><span>↓</span></div><p>${esc(file.description)}</p><small>下载 Markdown / YAML</small></a>`).join("")}</div></section>`;
const sourceLine=`<aside class="doc-source-line"><span>本页依据</span><a href="${articleUrl}" target="_blank" rel="noreferrer">Anthropic 原文</a><i>·</i><a href="${officialDocsUrl}" target="_blank" rel="noreferrer">Claude Code 官方文档</a><small>中文内容为原创解读与实践归纳</small></aside>`;
for(const [index,doc] of docs.entries()){
  const shallow=doc.slug==="concepts";
  const scope=shallow?"shallow":"doc";
  const prefix=shallow?"../":"../../";
  const dir=shallow?join(out,"guide"):join(out,"guide",doc.slug);
  mkdirSync(dir,{recursive:true});
  const prev=docs[index-1],next=docs[index+1];
  const progress=Math.round(((index+1)/docs.length)*100);
  const toc=`<nav class="doc-toc" aria-label="本页目录"><span>本页目录</span><div>${doc.sections.map((item,i)=>`<a href="#section-${i+1}">${String(i+1).padStart(2,"0")} ${esc(item.heading)}</a>`).join("")}</div></nav>`;
  const actions=`<div class="doc-actions"><a class="tool-button" href="${prefix}guide/templates/index.html">下载配套模板</a><button class="tool-button" type="button" onclick="window.print()">打印 / 导出 PDF</button></div>`;
  const pager=`<nav class="pager">${prev?`<a href="${docHref(scope,prev)}"><small>上一篇</small><b>← ${esc(prev.title)}</b></a>`:"<span></span>"}${next?`<a href="${docHref(scope,next)}"><small>下一篇</small><b>${esc(next.title)} →</b></a>`:"<span></span>"}</nav>`;
  const body=`<div class="docs-shell">${sidebar(doc.slug,scope,prefix)}<article class="doc"><div class="crumb"><a href="${prefix}index.html">首页</a><span>/</span><a href="${shallow?"index.html":"../index.html"}">文档</a><span>/</span>${esc(doc.title)}</div><p class="doc-kicker">${esc(doc.number)} · ${esc(doc.kicker)}</p><h1>${esc(doc.title)}</h1><p class="doc-summary">${esc(doc.summary)}</p><div class="doc-meta"><span>第 ${index+1} / ${docs.length} 篇</span><span>约 ${readMinutes(doc)} 分钟</span><span>${doc.sections.length} 个主题</span></div><div class="doc-progress" aria-label="阅读进度 ${progress}%"><span style="width:${progress}%"></span></div>${sourceLine}${toc}${actions}${doc.slug==="templates"?templateDownloads(prefix):""}${doc.sections.map(section).join("")}${pager}</article></div>`;
  const canonical=shallow?"guide/":"guide/"+doc.slug+"/";
  writeFileSync(join(dir,"index.html"),head(`${doc.title} · AI 原生软件开发生命周期中文手册`,doc.summary,prefix,canonical)+top(prefix,doc.slug==="templates"?"templates":doc.slug==="sources"?"sources":"docs")+body+footer+behaviorMarkup+"</body></html>");
}

const stages=["plan","design","build","test","deploy","maintain"].map(slug=>docs.find(doc=>doc.slug===slug)).filter(Boolean);
const cards=stages.map((doc,index)=>`<a class="stage-card" href="guide/${doc.slug}/index.html"><span class="stage-no">${String(index+1).padStart(2,"0")}</span><h3>${esc(doc.title.split("：")[0])}</h3><p>${esc(doc.summary)}</p><b>阅读 →</b></a>`).join("");
const routes=[["产品与设计","先把问题、约束和验收写清楚","guide/plan/index.html","计划 → 设计"],["工程团队","让计划、代码和测试形成反馈闭环","guide/build/index.html","构建 → 测试"],["平台与安全","把权限、沙箱、Hooks 和 CI 变成门","guide/gates/index.html","治理控制"],["管理者与值班","看懂责任、指标和生产回流","guide/governance/index.html","治理 → 维护"]].map(([title,description,href,label])=>`<a class="route-card" href="${href}"><span>${label}</span><h3>${title}</h3><p>${description}</p><b>从这里开始 →</b></a>`).join("");
const home=`<main><section class="hero shell"><div class="eyebrow"><span class="signal"></span>非官方中文学习手册 · 2026</div><h1>代码写快以后，<br><span>研发流程反而堵了。</span></h1><p class="lede">Anthropic 的 AI 原生软件开发生命周期，真正改造的不是一段代码，而是计划、设计、构建、测试、部署和维护之间的整条交接链。</p><div class="actions"><a class="primary" href="guide/index.html">开始阅读</a><a class="secondary" href="${articleUrl}" target="_blank" rel="noreferrer">核对英文原文 ↗</a><a class="secondary" href="guide/templates/index.html">下载模板</a></div><div class="bottleneck" aria-label="瓶颈迁移示意"><div><small>旧瓶颈</small><strong>BUILD</strong><span>人工写代码</span></div><i>→</i><div class="hot"><small>新瓶颈</small><strong>PLAN · TEST · DEPLOY</strong><span>交接、验证与批准</span></div></div></section><section class="section shell"><div class="section-head"><span>THE LOOP</span><h2>六个阶段，一条能回流的产物链</h2></div><div class="stage-grid">${cards}</div></section><section class="section shell routes-section"><div class="section-head"><span>CHOOSE A ROUTE</span><h2>按你的责任，挑一条入口</h2></div><div class="route-grid">${routes}</div></section><section class="section shell dependency-section"><div class="section-head"><span>DEPENDENCY MAP</span><h2>先建立什么，后放开什么</h2></div><div class="dependency-map"><div><small>01 · 共同真源</small><strong>intent / spec / plan</strong><p>先让每一步都有可追溯的输入与产物。</p></div><i>→</i><div><small>02 · 可判错的反馈</small><strong>tests / evals / review</strong><p>再让智能体知道什么叫完成、哪里必须停。</p></div><i>→</i><div><small>03 · 受控的行动</small><strong>hooks / sandbox / CI</strong><p>最后才增加并行、写入和自动触发。</p></div></div></section><section class="artifact shell"><div><span>COMMITTED ARTIFACTS</span><h2>聊天会结束，产物必须留下来</h2><p>每一阶段都写下一个人和智能体都能读取的版本化产物。提交记录保存谁提出、谁修改、谁批准，以及下一步为什么被触发。</p><a class="inline-link" href="guide/templates/index.html">查看并下载 6 个模板 →</a></div><ol><li><code>intent.md</code><em>问题与结果</em></li><li><code>spec.md</code><em>需求与约束</em></li><li><code>plan.md</code><em>文件、顺序、验证</em></li><li><code>diff + tests</code><em>实现与证据</em></li><li><code>PR + incident</code><em>批准与回流</em></li></ol></section><section class="section shell update-section"><div class="update-panel"><div><span>LAST UPDATED · 2026-08-26</span><h2>这次更新了什么</h2><p>补齐原文主题覆盖矩阵，扩展真源、Skills、Hooks、持续评测、PR 评审、企业托管、CI/CD 与维护闭环，并加入可下载模板、搜索和打印入口。</p></div><div class="update-list"><div><b>覆盖</b><span>14 篇文档 · 42 个主题</span></div><div><b>边界</b><span>原创解读，不是官方译本</span></div><div><b>反馈</b><span>欢迎在 GitHub 提交修正建议</span></div><div class="repo-links"><a href="https://github.com/huiyonghkw/ai-native-sdlc-guide" target="_blank" rel="noreferrer">Star / Fork ↗</a><a href="https://github.com/huiyonghkw/ai-native-sdlc-guide/issues/new" target="_blank" rel="noreferrer">提交反馈 ↗</a></div></div></div><div class="share-panel"><div><span>SHARE THIS GUIDE</span><h2>把入口带回团队</h2><p>适合放进项目 README、团队知识库或公众号文章末尾。链接始终指向可更新的 GitHub Pages。</p><a class="inline-link" href="share-copy.md" download>下载公众号发布素材 →</a></div><div class="share-tools"><button class="tool-button" type="button" data-copy-page>复制页面链接</button><button class="tool-button tool-button-dark" type="button" data-share-page>分享这页</button></div></div></section></main>`;
writeFileSync(join(out,"index.html"),head("AI 原生软件开发生命周期中文手册","完整理解 Anthropic AI-Native SDLC playbook 的非官方中文学习手册。","","",true)+top("","product")+home+footer+behaviorMarkup+"</body></html>");

mkdirSync(join(out,"assets"),{recursive:true});
writeFileSync(join(out,"assets/styles.css"),readFileSync(join(root,"app/globals.css"),"utf8").replace('@import "tailwindcss";',"").replaceAll('url("/fonts/','url("../fonts/'));
writeFileSync(join(out,".nojekyll"),"");
try{cpSync(join(root,"public/og.png"),join(out,"og.png"))}catch{ /* optional social image */ }
try{cpSync(join(root,"public/fonts"),join(out,"fonts"),{recursive:true})}catch{ /* optional local fonts */ }
try{cpSync(join(root,"public/templates"),join(out,"templates"),{recursive:true})}catch{ /* optional templates */ }
try{cpSync(join(root,"public/share-copy.md"),join(out,"share-copy.md"))}catch{ /* optional publishing copy */ }
const urls=["",...docs.map(doc=>`guide/${doc.slug===`concepts`?"":doc.slug}`)].map(path=>`${siteUrl}/${path}`);
writeFileSync(join(out,"sitemap.xml"),`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(url=>`<url><loc>${url}</loc></url>`).join("")}</urlset>`);
writeFileSync(join(out,"robots.txt"),`User-agent: *\nAllow: /\nSitemap: ${siteUrl}/sitemap.xml\n`);
console.log(`Built ${docs.length+1} static pages in pages-dist`);
